// ====================================================================
// NFE MANAGER — ORQUESTRADOR CENTRAL & BOOTSTRAP DO FRONTEND
// ====================================================================

document.addEventListener("DOMContentLoaded", () => {
    if (typeof init === "function") {
        init();
    }
});
